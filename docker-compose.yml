version: "3.9"
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: hasslepromo
      POSTGRES_USER: user
      POSTGRES_PASSWORD: rewkopjk)_IKpj34po2jOJKE32
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  bot:
    build: .
    env_file:
      - .env
    depends_on:
      - postgres
    volumes:
      - .:/app

volumes:
  pgdata: